name: apiverve_caaparser
description: CAA Record Parser analyzes DNS CAA records that specify which Certificate Authorities are authorized to issue certificates for a domain.
version: 1.1.14
homepage: https://apiverve.com/marketplace/caaparser?utm_source=dart&utm_medium=homepage
repository: https://github.com/apiverve/caaparser-api/tree/main/dart

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  http_parser: ^4.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
